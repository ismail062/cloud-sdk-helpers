{
  "name": "cloud-sdk-helpers",
  "version": "1.0.0",
  "description": "cloud helpers to use accross functions. ",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "muhammad.ismail@novuna.co.uk",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.556.0",
    "@aws-sdk/lib-dynamodb": "^3.556.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
